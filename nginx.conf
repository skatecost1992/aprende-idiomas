server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  # SPA: cualquier ruta desconocida sirve index.html (la app es de una sola página).
  location / {
    try_files $uri $uri/ /index.html;
  }
}
